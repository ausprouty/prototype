function show_notes() {
  var text = "this will show notes ";
  return text;
}

function addNote(){
	console.log('In add_note');
	var notesPage = document.getElementById("notes_page").value;
	console.log('Notes Page' + notesPage.value);
	// find ids of all textareas
	var txtAreas = document.getElementsByTagName("textarea");
	var len = txtAreas.length;
	var ids = new Array();
	var item = new Array();
	for(i=0; i<len; i++)
	{
		ids.push(txtAreas[i].id);    
	}
	for (var i = 0; i < len; i++) {
		note = document.getElementById(ids[i]);
		var entry = new Object();
		entry.key = ids[i];
		entry.value = note.value;
		console.log(entry);
		item[i] = entry;
	  }

  localStorage.setItem(notesPage, JSON.stringify(item)); //put the object back
}
